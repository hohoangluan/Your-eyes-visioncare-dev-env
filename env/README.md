# Environment templates

Mẫu biến local cho từng repository. Nguồn đầy đủ là `.env.example` ở root repository tương ứng; file tại đây chỉ override môi trường local. Giá trị dev phải đánh dấu **DEV ONLY** (§14.2).
