# manifests

Định nghĩa service local với build context sibling `../Your-eyes-visioncare-host` và `../Your-eyes-visioncare-smart-glasses` (§9.4). Mobile chạy ngoài container trên emulator/thiết bị thật và kết nối Host local.

**Chưa có manifest nào**; §9.4 chưa khóa container runtime cho toàn bộ.
