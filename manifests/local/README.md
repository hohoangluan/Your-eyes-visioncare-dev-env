# local

Service phát triển hằng ngày dự kiến: postgres, redis, host, ocr-mock, vlm-mock, glasses-simulator (§9.4). Image phải pin version, không dùng `latest`; service phải có healthcheck trước integration test.
